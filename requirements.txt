# Installed via apt by install.sh (the trixie image has no pip3):
#   sudo apt install python3-yaml python3-rpi.gpio
# picamera backend, also via apt:
#   sudo apt install python3-picamera2
RPi.GPIO>=0.7.1
pyyaml>=6.0
